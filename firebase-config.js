/* =========================================================================
 * Firebase settings — paste your own project's values here.
 * Step-by-step instructions are in SETUP.md.
 *
 * Until you fill this in, the organiser works exactly as it does now:
 * everything is saved on this device only, with no sign-in and no cloud.
 *
 * These values are NOT secret. Google intends them to be public in web
 * apps — your data is protected by the Firestore security rules in
 * SETUP.md, which only let you read and write your own organiser.
 * ========================================================================= */

window.FIREBASE_CONFIG = {
  apiKey: "PASTE_API_KEY_HERE",
  authDomain: "PASTE_PROJECT_ID.firebaseapp.com",
  projectId: "PASTE_PROJECT_ID",
  appId: "PASTE_APP_ID",
};
